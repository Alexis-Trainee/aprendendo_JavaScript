const raio = 5.6;
const area = Math.PI * Math.pow(raio,2); //PI é uma constante e POW que vem de (POWER) é uma função.
console.log(area);
console.log(typeof Math);//Vai retornar objeto pois Math não é uma função.  
